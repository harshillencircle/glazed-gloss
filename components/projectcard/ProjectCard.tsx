import { storyblokEditable } from "@storyblok/react";

export default function ProjectCard({ blok }: any) {
    return (
        <section {...storyblokEditable}>
            <div></div>
        </section>
    );
}